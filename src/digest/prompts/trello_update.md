Write my midday Trello card update for {date}. It goes to my team board, so it
must be paste-ready, short, first person, and technically grounded. Most of my
work happens in the morning, so the evidence from today's sessions is the core
material; late-yesterday evidence is context only.

How to read the evidence before writing:
1. Look at the "User requests" lines first, newest sessions first. They carry
   the asks, corrections, and decisions; the actions and issues support them.
2. Merge all of today's sessions into ONE workstream arc. Do not summarize
   session by session.
3. Only the PRIMARY repo's workstream goes on the card unless a secondary repo
   had real work.

Produce exactly these two sections, each as a "## " heading:

## Bigger picture
2-3 sentences on where the workstream stands as a whole: what this effort is,
what changed about its state today, and what the real remaining problem is.
Think "what would I tell my tech lead in 20 seconds", not a list of edits. No citations
needed here, but every claim must still come from the evidence.

## Today's Update
The card body, in this exact shape (drop lines that do not apply, one sentence
per line, max 5 lines total):

- I completed [work done] around [module / area].
- The main finding is [specific technical finding].
- I'm now moving toward [next implementation step].
- Blocker / open question: [only if real].

Rules for the card:
- Backtick real technical names: functions, files, prompts, configs, branches.
- Concrete findings, not generic progress. Never "made significant progress"
  or "worked on multiple aspects".
- A discovery pass is a discovery pass; do not report it as shipped work.
- If something is uncertain, say so plainly inside the line ("not re-run yet",
  "still unverified") instead of adding extra sections.
- No citations inside the card (it gets pasted into Trello); the Sources list
  is appended outside the card automatically.

EVIDENCE:

{evidence}
